# Это комментарий, который говорит, что переменная CC указывает компилятор, используемый для сборки
CC=g++
CFLAGS=-lhwsrv -lpthread 

all: 

	$(CC)  check.cpp -o check $(CFLAGS)
	$(CC)  otch.cpp -o otch $(CFLAGS)
	$(CC)  timeSynchronization.cpp -o timeSynchronization $(CFLAGS)
